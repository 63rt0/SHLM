class Pistola extends Modelo {

    constructor(x, y/*, grados*/) {
        super(imagenes.pistola, x, y);
        //this.animacion = new Animacion(imagenes.pistola,20,20,1,1);
        //this.grados=grados;
    }
/*
    dibujar (scrollX, scrollY){

        console.log("hola");
        scrollX = scrollX || 0;
        scrollY = scrollY || 0;
        this.animacion.dibujar(this.x - scrollX, this.y - scrollY,this.grados);
    }*/

}
